{
  "format": "layercake-phase2-byte-instruction-finetune/1",
  "candidate_name": "sliding16_cosine_promptpointer32_step400",
  "seed": 20260814,
  "instruction_corpus": "data/moonshot/phase2/instruction_distillation_curated.jsonl",
  "model_overrides": {
    "prompt_pointer_dim": 32
  },
  "training": {
    "steps": 400,
    "batch_size": 2,
    "wiki_batch_size": 2,
    "sequence_bytes": 1280,
    "learning_rate": 0.001,
    "weight_decay": 0.01,
    "gradient_clip": 1.0,
    "instruction_weight": 1.0,
    "wiki_weight": 0.0,
    "prompt_separator": "none",
    "prompt_contrastive_weight": 0.0,
    "prompt_focus_weight": 4.0,
    "prompt_focus_contrastive_weight": 5.0,
    "prompt_focus_contrastive_margin_nats": 0.2,
    "routing_balance_weight": 0.0,
    "trainable_parameters": "prompt_pointer"
  },
  "budget": {
    "maximum_runs": 1,
    "maximum_steps": 400,
    "maximum_wall_seconds": 600,
    "maximum_cuda_memory_bytes": 12884901888
  },
  "hypothesis": "Cross-attention can identify disjoint topic spans under teacher forcing but cannot emit them autonomously through the frozen byte head. A learned pointer-generator mixes the base neural byte distribution with a neural attention distribution scattered over cached raw prompt bytes, giving unseen lexical content a direct differentiable emission path without templates, forced bytes, external retrieval, or a different checkpoint."
}
